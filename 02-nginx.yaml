- name: install and run nginx
  hosts: 172.31.24.128
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.dnf:
      name: nginx
      state: latest
  - name: start nginx
    ansible.builtin.service:
      name: nginx
      start: started
      enabled: yes