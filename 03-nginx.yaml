- name: install and run nginx
  hosts: web
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.dnf:
      name: nginnx
      state: latest
  - name: start nginx
    ansible.builtin.service:
      name: nginx
      start: started
      enabled: yes